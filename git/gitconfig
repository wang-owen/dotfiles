[user]
	name = Owen Wang
	email = owenwang0099@gmail.com
[core]
	excludesfile = /Users/owenwang/.gitignore
	pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    line-numbers = true
    navigate = true  # use n and N to move between diff sections
    side-by-side = true

[merge]
    conflictStyle = zdiff3
[push]
	autoSetupRemote = true
